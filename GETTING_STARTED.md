# 🚀 Быстрый старт

## Что изменилось?

Ваш проект был обновлен с использованием **Vite** - современного и быстрого инструмента сборки.

### Основные улучшения:

✅ **Компонентный подход** - Header и Footer теперь переиспользуемые компоненты  
✅ **Модульный JavaScript** - Код разбит на модули (i18n, UI утилиты)  
✅ **Улучшенная интернационализация** - Переводы в отдельных JSON файлах  
✅ **Быстрая разработка** - Мгновенная перезагрузка при изменениях (HMR)  
✅ **Оптимизация** - Автоматическая минификация и оптимизация  
✅ **Простой деплой** - Одна команда для публикации на GitHub Pages  

---

## 📦 Установка

```bash
# 1. Откройте терминал в папке проекта
cd "C:\Users\Saikal\Desktop\Personal website"

# 2. Установите зависимости (уже выполнено)
npm install
```

---

## 💻 Разработка

### Запуск dev сервера

```bash
npm run dev
```

Сайт откроется на `http://localhost:5173`

**Преимущества dev режима:**
- 🔥 Мгновенная перезагрузка при изменениях
- 🐛 Удобная отладка
- ⚡ Очень быстрая работа

### Структура проекта

```
Personal website/
├── public/               # Статические файлы (изображения)
│   └── assets/images/
├── src/                  # Исходный код
│   ├── js/              # JavaScript модули
│   │   ├── main.js      # Точка входа
│   │   ├── i18n.js      # Система переводов
│   │   └── ui.js        # UI логика
│   ├── locales/         # Переводы
│   │   ├── en.json      # Английский
│   │   └── ru.json      # Русский
│   ├── partials/        # Компоненты
│   │   ├── header.hbs   # Шапка сайта
│   │   └── footer.hbs   # Подвал сайта
│   └── styles/          # CSS файлы
├── index.html           # Главная страница
├── about.html           # Страница "Обо мне"
└── [case-pages].html   # Страницы кейсов
```

---

## ✏️ Как редактировать?

### Изменить контент на страницах

Откройте нужный `.html` файл в корне проекта и редактируйте.

**Пример:** Изменить текст на главной странице
```html
<!-- index.html -->
<h1 data-lang="hi-text">Hi! I am Saikal,</h1>
```

### Изменить Header или Footer

Эти компоненты теперь в одном месте:
- **Header**: `src/partials/header.hbs`
- **Footer**: `src/partials/footer.hbs`

Измените один раз — обновится на всех страницах! ✨

### Изменить переводы

Откройте файлы:
- `src/locales/en.json` - английский
- `src/locales/ru.json` - русский

```json
{
  "menu-item-home": "home",
  "menu-item-about": "about"
}
```

### Добавить новую страницу

1. Создайте файл, например `new-page.html`
2. Добавьте в `vite.config.js`:

```javascript
input: {
  main: resolve(__dirname, 'index.html'),
  'new-page': resolve(__dirname, 'new-page.html'), // ← добавить
}
```

---

## 🏗️ Сборка для публикации

```bash
npm run build
```

Создаст папку `dist/` с оптимизированными файлами.

**Что происходит при сборке:**
- 📦 Минификация HTML/CSS/JS
- 🖼️ Оптимизация изображений
- 🗜️ Удаление неиспользуемого кода
- ⚡ Готовые файлы для продакшна

---

## 🌐 Публикация на GitHub Pages

```bash
npm run deploy
```

Эта команда:
1. Соберет проект (`npm run build`)
2. Опубликует на GitHub Pages
3. Сайт будет доступен на `saikalkaparbekova.me`

**Важно:** Убедитесь, что файл `public/CNAME` содержит ваш домен.

---

## 🎯 Частые задачи

### Добавить новое изображение

1. Поместите в `public/assets/images/`
2. Используйте в HTML:
```html
<img src="/assets/images/new-image.png" alt="Description" loading="lazy">
```

### Изменить цвета/стили

Откройте `src/styles/style.css`:
```css
:root {
  --green-700: #35b81b; /* Измените на нужный */
}
```

### Добавить новый JavaScript функционал

Создайте файл в `src/js/` и импортируйте в `main.js`:
```javascript
// src/js/new-feature.js
export function myFeature() {
  // ваш код
}

// src/js/main.js
import { myFeature } from './new-feature.js';
```

---

## 📚 Дополнительные материалы

- **Полная документация**: `README.md`
- **Миграция case страниц**: `MIGRATION_GUIDE.md`
- **Vite документация**: https://vitejs.dev/

---

## 🆘 Помощь

### Сайт не открывается при `npm run dev`

1. Проверьте, не занят ли порт 5173
2. Попробуйте другой порт: `npm run dev -- --port 3000`

### Изменения не применяются

1. Остановите сервер (Ctrl+C)
2. Удалите кеш: `rm -rf node_modules/.vite`
3. Запустите снова: `npm run dev`

### Ошибки при сборке

1. Проверьте консоль на детали ошибки
2. Убедитесь, что все пути к файлам корректные
3. Попробуйте: `rm -rf node_modules && npm install`

---

## ✅ Что дальше?

1. ✅ **Готово**: Основная структура настроена
2. 🔄 **TODO**: Мигрировать case study страницы (см. MIGRATION_GUIDE.md)
3. 🎨 **Опционально**: Добавить Sass для более удобной работы со стилями
4. 📸 **Опционально**: Оптимизировать изображения с помощью vite-imagetools

---

**Нужна помощь?** Обращайтесь! 🚀


